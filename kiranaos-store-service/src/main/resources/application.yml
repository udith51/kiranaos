server:
  port: 8082

spring:
  application:
    name: kiranaos-store-service
  datasource:
    url: jdbc:postgresql://db.shtsascslrhehnisitvl.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: lbTEnRvbS2teSk3R
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true